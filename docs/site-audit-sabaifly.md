# Site Audit (Quick Rebuild)

- Files: **141**
- Size (MB): **4.82**
- HTML pages: **40**
- JS files: **48**
- CSS files: **9**

Top 100 largest files → see `largest-100-files.csv`.
Images ≥300KB → see `large-images.csv`.

Head blocking scripts (first pass) → `head-blocking-scripts.csv`.
Images missing alt (sample) → `missing-alt-samples.csv`.
